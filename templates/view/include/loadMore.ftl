<#if  (pageNum > 0)>
    <div class="u-textAlignCenter loadmore loadmore-home" pageNum="${pageNum !}" pageSize="${pageSize !}" search="${search !}" second="${second !}" clazz="${clazz !}">
        <button class="button button--pretty js-button" data-paged="2" data-action="laodpost">加载更多</button>
    </div>
</#if>